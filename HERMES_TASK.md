# Task for Hermes
**Goal:** We need to upgrade the Minimalist Chess game.
1. Read the current `script.js` and `index.html`.
2. Implement a fully functional chess timer (e.g., 5 minutes per player).
3. Display the timer cleanly on the UI.
4. When you are finished, run `git add .` and `git commit -m "Added chess timer"` to save the changes.
